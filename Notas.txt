Paso a paso para agregar el scss

1)

Ctrl+alt+ñ
npm init


2)
Comando para instalar el sass
npm install node-sass nodemon

3)
En el pacage json debemos de indicarle en donde esta la ubicacion para que conecete con el sass

"build-css": "node-sass --include-path scss scss/styles.scss css/style.css",
"watch-css": "nodemon -e scss -x \"npm run build-css\"",

De esta manera sabemos que en los styles vamos a tener lo necesario del sass 

4)
creamos las carpetas donde estaran las ubicaciones de los estilos y luego de lo mismo se generara 

Creamos el build para que arme los archivos

5)
npm run build-css


5)

Corremos la ejecucion necesaria

npm run watch-css



